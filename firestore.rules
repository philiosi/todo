rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // 개발 중에는 true로 설정, 실제 서비스에서는 인증 규칙 추가 필요
    }
  }
}